# TODO

## Completed
- Analyze ESLint failure in `frontend/src/pages/Admin/AdminPage.tsx`.
- Reworked `frontend/src/pages/Admin/AdminPage.tsx` so imports are at the top (fix `import/first`).
- Verified `npm run build` compiles successfully (warnings only).

## Next
- [ ] Optionally clean up remaining ESLint warnings in other files (not build-breaking).

